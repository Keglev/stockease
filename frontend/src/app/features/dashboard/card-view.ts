/** Which half of a card is on screen; the cards open on their chart, the at-a-glance reading. */
export type CardView = 'chart' | 'table';

export const CARD_VIEWS: readonly CardView[] = ['chart', 'table'];
