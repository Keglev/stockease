/**
 * The application's outward links, in one place because two surfaces now claim them: the footer
 * on every page and the landing page's credibility band. Duplicating the literals would let the
 * two drift, and a landing page pointing at a stale docs host is worse than no link at all.
 */
export const REPOSITORY_URL = 'https://github.com/Keglev/stockease';
export const DOCUMENTATION_URL = 'https://keglev.github.io/stockease/';
