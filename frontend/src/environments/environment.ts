export const environment = {
  apiBaseUrl: 'https://stockeasebackend.koyeb.app',
  // The flag marks the deployment, not the user: this build IS the demo, so it dies naturally
  // the day a non-demo deployment exists rather than needing to be reasoned about per session.
  demo: true
};
