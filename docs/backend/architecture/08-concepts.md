# Cross-cutting Concepts

> **Status: skeleton.** This section is planned and will be filled in a
> dedicated documentation PR. The outline below fixes its intended scope.

Planned content:
- In-process application events: synchronous listeners, deletion vetoes with listener ordering
- Pessimistic locking on the quantity write path
- Derived-versus-stored doctrine: snapshots stored, totals and overdue derived
- Soft delete with partial unique indexes; restore semantics
- Schema validation only (ddl-auto=validate)

[Back to Architecture Index](index.md)
